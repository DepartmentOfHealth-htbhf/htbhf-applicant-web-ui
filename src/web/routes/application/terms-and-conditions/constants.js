const CLAIMS_ENDPOINT = `/v2/claims`
const NO_ELIGIBILITY_STATUS_MESSAGE = 'The claimant service did not return an eligibility status'

module.exports = {
  CLAIMS_ENDPOINT,
  NO_ELIGIBILITY_STATUS_MESSAGE
}
